spring:
  application:
    name: 注册中心...
server:
  port: 8000
eureka:
  client:
    allow-redirects: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/


